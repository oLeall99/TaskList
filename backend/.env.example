# Database Configuration
DATABASE_URL=postgresql://tasklist_user:password@host:port/tasklist
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=admin
DB_NAME=tasklist_l2ih
DB_PORT=5432

# Server Configuration (Render usa porta 10000)
PORT=10000

# JWT Secret (IMPORTANTE: Use uma chave forte em produção)
JWT_SECRET=your-super-secret-jwt-key-change-this-to-something-strong

# Environment
NODE_ENV=production

# Frontend URL for CORS (production)
FRONTEND_URL=https://your-frontend-name.vercel.app 